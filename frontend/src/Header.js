import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <div className="Header">
        <h1>Stationeers Version History</h1>
        <p>
          This website shows the version history as available within the Stationeers main menu, for both the main release and beta branch.
        </p>
      </div>
    );
  }
}

export default Header;
